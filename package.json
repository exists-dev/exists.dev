{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev",
    "generate": "nuxi generate && touch .output/public/.nojekyll && cp CNAME .output/public/CNAME && rm .output/public/.DS_Store",
    "preview": "nuxi preview",
    "deploy": "gh-pages --dotfiles --dist dist",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.191",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.1.2",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "@nuxt/bridge": "^0.10.1",
    "@nuxtjs/tailwindcss": "^6.3.1",
    "@tailwindcss/typography": "^0.5.9",
    "axios": "^1.3.2",
    "daisyui": "^2.50.0",
    "lodash": "^4.17.21"
  }
}
